apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: LoadBalancer

  selector:
    app: ecom-frontend

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80